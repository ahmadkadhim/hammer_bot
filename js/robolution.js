// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    var keyMap;
    keyMap = {
      37: {
        direction: {
          strafe: +1
        },
        id: "left"
      },
      38: {
        direction: {
          forward: +1
        },
        id: "up"
      },
      39: {
        direction: {
          strafe: -1
        },
        id: "right"
      },
      40: {
        direction: {
          forward: -1
        },
        id: "down"
      }
    };
    $(document).keydown(function(e) {
      var direction, selected;
      direction = {};
      selected = keyMap[e.keyCode];
      $("#" + selected.id).addClass("selected");
      return $.ajax("http://127.0.0.1:8071/motion-control/update", {
        data: selected.direction,
        dataType: "jsonp"
      });
    });
    return $(document).keyup(function(e) {
      return $("#" + selected.id).removeClass("selected");
    });
  });

}).call(this);
