// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    var PressKeys, human, keyMap;
    keyMap = {
      37: {
        direction: {
          strafe: +1
        },
        id: "left"
      },
      38: {
        direction: {
          forward: +1
        },
        id: "up"
      },
      39: {
        direction: {
          strafe: -1
        },
        id: "right"
      },
      40: {
        direction: {
          forward: -1
        },
        id: "down"
      }
    };
    PressKeys = (function() {
      function PressKeys() {}

      $(document).keydown(function(e) {
        var direction, pressed;
        pressed = keyMap[e.keyCode];
        direction = {};
        $("#" + keyMap[e.keyCode].id).addClass("selected");
        return $.ajax("http://127.0.0.1:8071/motion-control/update", {
          data: keyMap[e.keyCode].direction,
          dataType: "jsonp"
        });
      });

      $(document).keyup(function(e) {
        return $("#" + keyMap[e.keyCode].id).removeClass("selected");
      });

      return PressKeys;

    })();
    return human = new PressKeys;
  });

}).call(this);
