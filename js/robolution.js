// Generated by CoffeeScript 1.6.3
(function() {
  $(document).ready(function() {
    var AjaxRequest, Dance, PressKeys, human, keyMap, moonwalk, server;
    keyMap = {
      37: {
        direction: {
          strafe: +1
        },
        id: "left"
      },
      38: {
        direction: {
          forward: +1
        },
        id: "up"
      },
      39: {
        direction: {
          strafe: -1
        },
        id: "right"
      },
      40: {
        direction: {
          forward: -1
        },
        id: "down"
      }
    };
    PressKeys = (function() {
      function PressKeys() {}

      $(document).keydown(function(e) {
        var direction, pressed;
        pressed = keyMap[e.keyCode];
        direction = {};
        $("#" + keyMap[e.keyCode].id).addClass("selected");
        return server.get(keyMap[e.keyCode].direction);
      });

      $(document).keyup(function(e) {
        return $("#" + keyMap[e.keyCode].id).removeClass("selected");
      });

      return PressKeys;

    })();
    AjaxRequest = (function() {
      function AjaxRequest() {}

      AjaxRequest.prototype.get = function(input) {
        return $.ajax("http://127.0.0.1:8071/motion-control/update", {
          data: input,
          dataType: "jsonp"
        });
      };

      return AjaxRequest;

    })();
    Dance = (function() {
      var moves;

      function Dance() {}

      moves = [0, 37, 38, 39, 40, 65, 68];

      Dance.prototype.start = function() {
        return console.log("cool");
      };

      return Dance;

    })();
    human = new PressKeys;
    server = new AjaxRequest;
    moonwalk = new Dance;
    return setInterval(function(duration) {
      return moonwalk.start();
    }, 1000);
  });

}).call(this);
